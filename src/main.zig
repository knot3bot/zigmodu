const std = @import("std");
const zigmodu = @import("root.zig");

pub fn main() !void {
    std.log.info("ZigModu Framework v0.7.0", .{});
    std.log.info("Usage: Use zigmodu as a library in your application", .{});
}
